package harness

// Version is the harness version. It is a single constant on purpose: product
// identity belongs in one place, so renaming or rebranding is a one-line change
// rather than an archaeology exercise across the tree.
const Version = "0.1.0"

// Name is the product name used in user-facing output.
const Name = "deepseek-pi"
